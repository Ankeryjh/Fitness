import 'package:flutter/material.dart';
class Boxerpage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Boxerpage'),
      ),
      body: Center(
        child: Text('This is the Boxerpage.'),
      ),
    );
  }
}