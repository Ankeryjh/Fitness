import 'package:flutter/material.dart';
class GoalsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ExerciseCalendarPage'),
      ),
      body: Center(
        child: Text('This is the ExerciseCalendarPage.'),
      ),
    );
  }
}